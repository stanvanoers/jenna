.row
  .col-3(col)
    each val in col0
      li!= val.get("project")
  .col-3(col)
    each val in col1
      li!= val.get("project")
  .col-3(col)
    each val in col2
      li!= val.get("project")
